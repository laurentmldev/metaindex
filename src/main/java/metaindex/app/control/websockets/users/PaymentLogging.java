package metaindex.app.control.websockets.users;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;


/**
 * Just used to forward corresponding message to separate log file with log4j
 * Possibly better way to do that ...
 * @author laurentml
 *
 */
public class PaymentLogging {
    	public static Log logger = LogFactory.getLog(PaymentLogging.class);    
    }