


$ sudo do apt-get install phpmyadmin

# set apache2 run group
$ vi /etc/apache2/envvars
      export APACHE_RUN_GROUP=XXX

$ sudo /etc/init.d/apache2 start
$ echo "<?php phpinfo(); ?>" > /var/www/html/index.php

firefox : http://localhost:80/index.php

$ cd /var/www/html && sudo ln -s /usr/share/phpmyadmin

firefox : http://localhost:80/phpmyadmin

/// configure apache2 / phpmyadmin

$ sudo apt-get install mysql-client php5-mysql

# set run group so that it can access the folder on external drive 
$ vi /etc/apache2/envvars

